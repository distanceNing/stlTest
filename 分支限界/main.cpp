#include <iostream>
#include <vector>
#include "Knapsack.h"
#include "MaxLoading.h"
#include "Graph.h"


int main()
{
	/*const char *filename = "data.txt";
	Graph test(filename);
	std::vector<int>data = {1,3,4};
	test.input_demand(0, 6, data);
	test.find_optimal_path();*/

	/*std::vector<int> w = {2,2,4,6,5,};
	std::vector<int> v = { 6,3,6,5,4 };
	Knapsack test(w, v, 10);
	test.SearchforBest();*/


	//std::vector<int>w = {0,10,20,20,40,50 };
	//MaxLoading test(w.size()-1, 110, 50, w);
	//test.SearchforBest();
	//

	return 0;
}