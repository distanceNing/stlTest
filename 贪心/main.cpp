#include <iostream>
#include"Prim.h"
#include"Dijkstra.h"
#include"HuffmanCode.h"
#include "Kruskal.h"
#include <vector>
int main()
{
	/*int graphSize=7;
	int **graph = new int*[graphSize];
	int rongliang = 5;
	for (int i = 0; i < graphSize; i++)
	{
		*(graph + i) = new int[graphSize];
	}
	readFile("E:/data.txt",graph,graphSize);*/
	////Dijkstra(1, 6, graph, graphSize);
	//Prim(graph, graphSize);
	//Kruskal(edge, graphSize);

	/*std::vector<int>value = {1,2,6,4,5};
	HuffTree test(value);
	test.PreorderTree();
	std::vector<int>huffCode;
	test.HuffManCode(4, huffCode);
	*/

	return 0;
}